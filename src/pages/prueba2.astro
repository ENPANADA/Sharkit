<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h2 class="border">wavy</h2>
    <h2 class="wave">wavy</h2>
    <h2 class="prueba">wavy</h2>

    <div id="container"></div>
    <style>
      div {
        width: 200px;
        height: 200px;
        background-color: #09f;
        background:
          no-repeat
            linear-gradient(to bottom, rgb(252, 7, 7) 70%, transparent 70%),
          radial-gradient(
            farthest-side at 30% 0%,
            blue 70%,
            rgb(0, 255, 34) 70%
          );
        background-size:
          100% 10%,
          60px 60px;
        background-position:
          0 0,
          50% 50%;
        /* background-repeat: no-repeat; */
        /* background-position: top; */
        box-shadow: 0 0 0 10px #333; /* Sombra para el borde negro */
      }

      h2 {
        font-size: 15vmin;
        position: absolute;
      }

      .border {
        top: 0;
        color: rgba(255, 0, 0, 0.384);
        background:
          linear-gradient(to bottom left, transparent 49.8%, #e0e6e5 50%) top /
            100% 35%,
          linear-gradient(to top left, transparent 49.8%, #e0e6e5 50%)
            bottom/100% 35%,
          linear-gradient(#e0e6e5, #e0e6e5) center/100% 30%;
        background-repeat: no-repeat;
        background-size: 50px 50px;
        /* background-clip: text; */
      }

      .prueba {
        bottom: 0;
        color: #09f;
      }

      .wave {
        color: #09f;
        animation: wave 3s ease-in-out infinite;
      }

      @keyframes wave {
        0%,
        100% {
          clip-path: polygon(
            0% 47%,
            10% 48%,
            33% 54%,
            54% 60%,
            70% 61%,
            84% 59%,
            100% 52%,
            100% 100%,
            0% 100%
          );
        }

        50% {
          clip-path: polygon(
            0% 60%,
            15% 65%,
            34% 66%,
            51% 62%,
            67% 50%,
            84% 45%,
            100% 46%,
            100% 100%,
            0% 100%
          );
        }
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
        background: black;
        display: flex;
        height: 100vh;
        width: 100vw;
        align-items: center;
        justify-content: center;
      }

      * {
        margin: 0;
        padding: 0;
      }
    </style>
  </body>
</html>
